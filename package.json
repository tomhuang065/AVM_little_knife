{
  "name": "hw7",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "start": "cd frontend && yarn start",
    "server": "cd backend && yarn server",
    "download": "cd frontend && yarn install --freeze-lockfile && cd ../backend && yarn install --freeze-lockfile",
    "deploy": "cd backend && yarn deploy",
    "build":"cd frontend && yarn build"
  },
  "dependencies": {
    "@material-ui/core": "^4.12.4",
    "axios": "^1.1.3",
    "styled-components": "^5.3.6"
  }
}
